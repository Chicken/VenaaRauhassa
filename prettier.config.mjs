/** @type {import('prettier').Config} */
const config = {
  plugins: [],
  printWidth: 100,
  tabWidth: 2,
  semi: true,
  singleQuote: false,
  quoteProps: "consistent",
  trailingComma: "es5",
  bracketSpacing: true,
  arrowParens: "always",
  endOfLine: "lf",
};

export default config;
